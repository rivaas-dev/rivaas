apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chart.fullname" . }}-config
  labels:
    {{- include "chart.labels" . | nindent 4 }}
data:
  {{- with .Values.config }}
  config.yml: |
    {{ toYaml . | nindent 4 }}
  {{- end }}
